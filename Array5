//Move all the negative elements to one side of the array


#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n=9, a[n];
    for (int i = 0; i < n; i++) {
        /* code */
        cin>>a[i];
    }
    std::deque<int> dq;
    for(int i=0; i<n; i++)
    {
        if(a[i]<0)
            dq.push_front(a[i]);
        else
            dq.push_back(a[i]);
    }
    for (int i = 0; i < n; i++) {
        /* code */
        cout<<dq[i]<<" ";
        
    }
}
